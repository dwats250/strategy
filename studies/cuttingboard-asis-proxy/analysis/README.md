# analysis/

Reproduction and analysis code for this study (`docs/conventions.md` §d).

**Empty: no run has been executed**, so there are no numbers to reproduce.

When a run exists, the reproduction script here must assert the headline figures this study
reports and fail loudly if they do not reproduce, print package versions, and print the checksum
of every input file it reads — the `reproduce_campaign.py` pattern from `spy-orb-first-break`.
