# Amendment — RUN_SPY_1D_2015-01-01, 2026-07-30 UTC

**Amends:** `RUN_SPY_1D_2015-01-01.md` (frozen at capture),
`../analysis/FINDINGS_RUN_SPY_1D_2015-01-01.md` (published), and the notes field of
`../LEDGER.csv` row 1. Per `docs/conventions.md` §b, none of those records is edited in place;
this dated amendment is the correction.

## The defect

Row counts for the two capture files were derived with `wc -l`, which counts newline characters.
Both TradingView exports lack a trailing newline, so each count was low by one. All classification
and funnel numbers were computed by field parsing (`awk`, record-based) and are unaffected.

## Canonical verification (2026-07-30, `awk 'END{print NR}'`, immune to the missing newline)

| File | Records | Data rows |
|---|---:|---:|
| `../exports/CBASIS_v0_1_AMEX_SPY_1D_RTH_20150101-20260729_048f5c66.csv` (SHA-256 `d1b53750…`) | 3,621 | **3,620** |
| Rejected first capture, archived outside the repository (SHA-256 `751295b1…`) | 868 | **867** |
| `../../../exploratory/.../exports/CBF05_BATS_SPY1D_V4_bars_20131212-20260728.csv` (SHA-256 `e28aa874…`) | 3,174 | **3,173** — matches its documentation; no correction needed |

The discrepancy was first surfaced by independent arithmetic in
`docs/gap-register-2026-07-29.md` (Appendix A) and confirmed against the canonical files.

## Corrections

Every statement of **3,619** exported rows reads as **3,620**, and the rejected capture's
**866** rows reads as **867**, in these records:

| Record | Location | Incorrect → correct |
|---|---|---|
| `RUN_SPY_1D_2015-01-01.md` | "Bars available at capture" cell | 3,619 → 3,620 bars emitted |
| `RUN_SPY_1D_2015-01-01.md` | all seven macro "Resolved?" cells | "votes_cast = 8 on all 3,619 exported rows" → all 3,620 |
| `FINDINGS_RUN_SPY_1D_2015-01-01.md` | §1.5 | "3,619 of the 8,431 bars" → 3,620 |
| `LEDGER.csv` row 1 | notes field | "partial window 866 rows" → 867 |
| `../README.md` | contents table, export row | "3,619 rows" → 3,620 (living document; correctable in a normal docs change) |
| git commit `93fc084` | message body | "3,619-row export" — immutable history; corrected here only |

## Unaffected

All declared-window results (2,909 bars evaluated; 602 hard-gate pass; 170 QUALIFIED, 125L/45S;
239 WATCHLIST; 204 kill-switch bars; the first-rejection distribution), the full-window
partition (200 / 297 / 3,123, kill 226, `votes_cast` = 8 on every row — now stated over 3,620
rows), every SHA-256, and the entire row-level v0.5 comparison (2,908 common bars, zero
disagreements). The ledger's authoritative count columns were always correct.
