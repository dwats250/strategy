# Proxy run manifest — RUN_SPY_1D_2015-01-01

Status: `PRE-REGISTERED — CAPTURE-TIME FIELDS COMPLETED BEFORE EXPORT, NEVER EDITED AFTER CAPTURE`

Created: 2026-07-28 UTC (template v0.1) — instantiated and pre-registered 2026-07-30 UTC

Instantiated from `RUN_MANIFEST_TEMPLATE_v0.1.md`. Capture-time fields are completed **before
capturing anything**, then the same run is recorded as one row in `../LEDGER.csv`. A field that
cannot be established is written `UNRECOVERABLE` — never left blank and never guessed.

> **Fill this before the export, not after.** UV02's friction scenario is permanently
> `UNRECOVERABLE` because Properties were never captured before export; seven otherwise-clean
> captures cannot support a friction-adjusted claim. The cost was not the missing field — it was
> that the field could not be recovered later at any price.

---

## Identity

| Field | Value |
|---|---|
| `run_id` | `RUN_SPY_1D_2015-01-01` |
| `run_date` (UTC) | TO BE FILLED AT CAPTURE — BEFORE EXPORT |
| `operator` | Dustin Watson (dwats250) |

## Source and script binding

| Field | Value |
|---|---|
| `cuttingboard_source_pin` | `59f8279d796335149afdec4aa507b6f927233518` |
| `script_file` | `scripts/cuttingboard_asis_proxy_v0_1.pine` |
| `script_sha256` | `048f5c66eefa3fdb8df9cec882006b1d8cf5fc9772d8694614559ba0a1bce3b5` |
| `rule_mapping_version` | `RULE_MAPPING_v0.1.md` |
| Script edited before this run? | no — SHA-256 re-verified 2026-07-29 against `scripts/cuttingboard_asis_proxy_v0_1.pine`: `048f5c66eefa3fdb8df9cec882006b1d8cf5fc9772d8694614559ba0a1bce3b5` |

## Chart and data convention

| Field | Value |
|---|---|
| `symbol` (exact TradingView ticker) | `AMEX:SPY` |
| `timeframe` | `1D` |
| `session` (regular / extended) | Regular trading hours |
| `timezone` (chart setting) | Exchange (America/New_York) |
| `chart_data_convention` (standard candles? adjusted? dividends?) | standard candles; split-adjusted (TradingView "adj" ON); dividend adjustment OFF |
| `data_provider / exchange feed` | TO BE FILLED AT CAPTURE — record the feed shown in the chart legend (plan-dependent) |
| `bar_timestamp_convention` (bar labels open or close) | bar labeled by session OPEN (TradingView daily CSV export emits Unix seconds at 09:30 ET bar open) |
| `date_window_start`, `date_window_end` | `date_window_start` = 2015-01-01 (evaluation window start; load maximum chart history so EMA50/ATR14/`OHLCV_MIN_BARS` warm-up completes well before this date; analysis counts use bars dated >= 2015-01-01 only). `date_window_end` = TO BE FILLED AT CAPTURE (capture date) |
| Bars available at capture | TO BE FILLED AT CAPTURE |

> Bar-timestamp convention and adjustment semantics are recorded explicitly. An off-by-one
> convention mismatch is indistinguishable from look-ahead in results.

## Macro series actually resolved

The 8-vote model requires all of these. Record which resolved and which returned `na`, because a
missing vote changes `bounded_net` and therefore the regime.

| Series | Ticker used | Resolved? |
|---|---|---|
| SPY | `AMEX:SPY` | TO BE FILLED AT CAPTURE — verify `votes_cast` = 8 in the Data Window on recent bars; record any series returning `na`. |
| QQQ | `NASDAQ:QQQ` | TO BE FILLED AT CAPTURE — verify `votes_cast` = 8 in the Data Window on recent bars; record any series returning `na`. |
| IWM | `AMEX:IWM` | TO BE FILLED AT CAPTURE — verify `votes_cast` = 8 in the Data Window on recent bars; record any series returning `na`. |
| VIX | `CBOE:VIX` | TO BE FILLED AT CAPTURE — verify `votes_cast` = 8 in the Data Window on recent bars; record any series returning `na`. |
| DXY | `TVC:DXY` | TO BE FILLED AT CAPTURE — verify `votes_cast` = 8 in the Data Window on recent bars; record any series returning `na`. |
| TNX | `TVC:TNX` | TO BE FILLED AT CAPTURE — verify `votes_cast` = 8 in the Data Window on recent bars; record any series returning `na`. |
| BTC | `BITSTAMP:BTCUSD` | TO BE FILLED AT CAPTURE — verify `votes_cast` = 8 in the Data Window on recent bars; record any series returning `na`. |

## TradingView capture details

| Field | Value |
|---|---|
| `tv_account` | TO BE FILLED AT CAPTURE — BEFORE EXPORT |
| `tv_plan` (affects bar limits) | TO BE FILLED AT CAPTURE — BEFORE EXPORT |
| `tv_capture_method` (Export chart data / screenshot / manual) | Export chart data (CSV), primary. If `display.data_window` series are absent from the export (mapping §3.2 declared-untested), fall back to the last-bar funnel table screenshot and record that here. |
| Any non-default chart setting | TO BE FILLED AT CAPTURE (expected: none) |
| Export file name | `exports/CBASIS_v0_1_AMEX_SPY_1D_RTH_20150101-<YYYYMMDD>_048f5c66.csv` — replace `<YYYYMMDD>` with the capture date (= `date_window_end`) |
| Export `sha256` | TO BE FILLED AFTER EXPORT |

## Declared limitations in force for this run

Restated per run so no reader has to reconstruct them:

- Gate 8 (MAX_RISK) and Gate 11 (TIME) are **NOT REPRESENTABLE** and are excluded from the soft
  count — never counted as passing.
- `EXPANSION` regime, CONTINUATION and PULLBACK_IMBALANCE entry modes are **not represented**.
- Chain validation and the five-step decision chain are **not represented**; the accepted path is
  therefore unobservable here, exactly as it was in the closed audit (EA-6-006).
- `volume_ratio` and `ema_spread_pct` are **approximations** — mapping §3.2.
- Whether TradingView's *Export chart data* emits `display.data_window` series is untested
  (mapping §3.2). If it does not, the last-bar funnel table supplies the counts; the substitution
  is recorded in `tv_capture_method`, not worked around.

## Result summary

| Field | Value |
|---|---|
| Bars evaluated | PENDING CAPTURE — completed from the verified export as part of the capture event, before this manifest freezes. |
| Hard-gate pass | PENDING CAPTURE — completed from the verified export as part of the capture event, before this manifest freezes. |
| Qualified | PENDING CAPTURE — completed from the verified export as part of the capture event, before this manifest freezes. |
| Watchlist | PENDING CAPTURE — completed from the verified export as part of the capture event, before this manifest freezes. |
| Kill-switch bars | PENDING CAPTURE — completed from the verified export as part of the capture event, before this manifest freezes. |
| First-rejection distribution | PENDING CAPTURE — completed from the verified export as part of the capture event, before this manifest freezes. |

## Amendment rule

Pre-registered per run and never edited after capture. A correction is a dated amendment file or
a new versioned manifest (`docs/conventions.md` §b).
