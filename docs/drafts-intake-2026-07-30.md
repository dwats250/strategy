# Intake note — program drafts of 2026-07-29

**Status:** `INTAKE RECORD — AUTHORIZES NOTHING.`

Created 2026-07-30 UTC. Applies to the three drafts committed alongside this note:

- `engine-program-draft-2026-07-29.md`
- `gap-register-2026-07-29.md`
- `infrastructure-and-capture-plan-2026-07-29.md`

1. **G-01 is closed.** The drafts were written against a pre-registration snapshot. The AS-IS
   baseline run was subsequently completed under the governed sequence: manifest pre-registered
   before capture, frozen at capture with all operator fields, export and screenshot registered,
   first `LEDGER.csv` row written, findings published
   (`studies/cuttingboard-asis-proxy/`, run `RUN_SPY_1D_2015-01-01`). The dated capture record
   G-01 requested is unnecessary; the row-count correction its arithmetic surfaced is recorded in
   `studies/cuttingboard-asis-proxy/manifests/RUN_SPY_1D_2015-01-01_AMENDMENT_2026-07-30.md`.
2. **The drafts authorize nothing.** They are retained verbatim as dated
   `DRAFT — OBSERVATIONAL` records, exactly as their own status lines state. Nothing in them is a
   standing rule, a charge, or a scope.
3. **Deferred pending an explicit owner charter:** the capture sprint and capture list, the
   Python metrics harness and any adoption of the external framework-gate vocabulary, all engine
   program phases (A, B, C, including `--dry-run --trace` and accepted-path observability), data
   acquisition under `DATA_PROVENANCE_CONTRACT.md`, and every `conventions.md` change the drafts
   propose (trial-budget fields, embargo rule, probe template, the §i boundary decision). Each
   proceeds only by its own explicit authorization.
